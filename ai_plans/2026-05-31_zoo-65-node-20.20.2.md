# Zoo PR #65 — chore(deps): update Node.js to v20.20.2

**Upstream:** Zoo-Code PR #65, merged 2026-05-12 (`da045d407`), author renovate[bot]
**Branch:** `feature/zoo-65-node-20.20.2` (off `main`)
**Type:** Chore / toolchain version bump — deps only, no code or test changes.

## Why PORT (despite triage SKIP)

The triage verdict was SKIP ("our deps/version flow owns these"), but the report's
decision line is `[x] PORT` — a genuine user override, so it appears in the `approved`
list and is the oldest unported approved PR (2026-05-12). We honor the explicit decision.

Keeping the pinned Node toolchain in lockstep with upstream is low-risk and avoids drift
in CI/`.nvmrc`/`engines`. The bump is a patch within the v20 line (20.19.2 → 20.20.2), so
no breaking-change exposure.

## What the upstream PR does

A renovate patch bump of the pinned Node version from **20.19.2 → 20.20.2** in four files:

| File               | Field                   | 20.19.2 → 20.20.2 |
| ------------------ | ----------------------- | ----------------- |
| `.nvmrc`           | whole file (`v20.19.2`) | `v20.20.2`        |
| `.tool-versions`   | `nodejs` line           | `nodejs 20.20.2`  |
| `package.json`     | `engines.node`          | `20.20.2`         |
| `src/package.json` | `engines.node`          | `20.20.2`         |

No source, test, or lockfile changes. `pnpm`/`vscode` engine pins are untouched.

## Scope in our fork (verified by current content)

All four files are at the upstream **pre-image** value `20.19.2` exactly (verified by grep),
so this is a clean apply — replace the version string in each. The upstream commit isn't
fetchable locally (no zoo remote), but the change is fully specified by the PR title and
diffstat (4 files, 4 single-line edits), and our base matches the pre-image.

Tumble naming: N/A — these are toolchain version pins, no brand strings.

## Plan

1. `.nvmrc`: `v20.19.2` → `v20.20.2`.
2. `.tool-versions`: `nodejs 20.19.2` → `nodejs 20.20.2`.
3. `package.json`: `engines.node` `20.19.2` → `20.20.2`.
4. `src/package.json`: `engines.node` `20.19.2` → `20.20.2`.

## Verification

- `grep 20.20.2` confirms all four updated; no stray `20.19.2` remains.
- Build gate: `pnpm install:vsix -y --editor=code` — green before push. (Local Node is
  v22; the engine warning is pre-existing and unrelated to this bump.)

## Credit

Co-authored-by: renovate[bot] <29139614+renovate[bot]@users.noreply.github.com>
