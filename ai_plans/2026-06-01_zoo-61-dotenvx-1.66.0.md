# Zoo PR #61 — Update @dotenvx/dotenvx to v1.66.0

- **Upstream:** Zoo-Code #61, squash `6c5d4e1ac`, merged 2026-05-14 19:06, author renovate[bot].
- **Branch:** `feature/zoo-61-dotenvx-1.66.0` (off `main`).
- **Credit:** `Co-authored-by: renovate[bot] <29139614+renovate[bot]@users.noreply.github.com>`.

## Change

Pure dependency bump. The root `package.json` already declares
`"@dotenvx/dotenvx": "^1.34.0"` (identical in both repos), so the only file the
upstream PR touched is `pnpm-lock.yaml` (resolved version bumped). Our lock currently
resolves `@dotenvx/dotenvx@1.44.2`; upstream targets `1.66.0`, which satisfies the
existing `^1.34.0` range — no `package.json` edit required.

## Approach

Regenerate the lockfile only, pinning the resolution to `1.66.0` to match upstream
intent (rather than `--latest`, which could pull a newer release):

```
pnpm update @dotenvx/dotenvx@1.66.0 --lockfile-only
```

Verify the diff is scoped to `@dotenvx/dotenvx` and its transitive deps — no unrelated
churn. Confirm the lock resolves `1.66.0`.

## Verification

- Lock resolves `@dotenvx/dotenvx@1.66.0`; diff limited to dotenvx + its deps.
- Build gate: `pnpm install:vsix -y --editor=code` must be green before push.
