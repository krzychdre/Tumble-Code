// Moved to @roo-code/core (PKG-6); this stub keeps the `@roo/api` and
// `shared/api` import paths working during the migration.
export {
	shouldUseReasoningBudget,
	shouldUseReasoningEffort,
	DEFAULT_HYBRID_REASONING_MODEL_MAX_TOKENS,
	DEFAULT_HYBRID_REASONING_MODEL_THINKING_TOKENS,
	GEMINI_25_PRO_MIN_THINKING_TOKENS,
	getModelMaxOutputTokens,
} from "@roo-code/core/browser"
export type { ApiHandlerOptions, FetchableModelSourceId, GetModelsOptions } from "@roo-code/core/browser"
