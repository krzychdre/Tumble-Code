export const extensionAliases = Object.freeze({
	// Node gives its deprecated built-in `punycode` module precedence over the
	// package with the same name. The trailing slash selects Punycode.js.
	punycode: "punycode/",
})
